# Copy to update-config.env (gitignored), then fill in your production values.
# Public metadata only; Sparkle's private key stays in your login Keychain.
SPARKLE_FEED_URL="https://your-domain.example/updates/appcast.xml"
SPARKLE_PUBLIC_KEY=""
# Optional: use your Developer ID certificate for signed, notarized releases.
# CODE_SIGN_IDENTITY="Developer ID Application: Your Name (TEAMID)"
# NOTARYTOOL_PROFILE="WhatThePort"
